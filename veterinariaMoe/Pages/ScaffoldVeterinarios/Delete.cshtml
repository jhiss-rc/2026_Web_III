@page
@model veterinariaMoe.Pages_ScaffoldVeterinarios.DeleteModel
@{
    ViewData["Title"] = "Eliminar Veterinario";
}

<div class="encabezado-pagina">
    <div class="container">
        <h1 class="titulo-pagina">Eliminar Veterinario</h1>
    </div>
</div>

<div class="container seccion-contenido">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="registro-contenedor">
                <h5 class="titulo-panel">¿Seguro que deseas eliminar este veterinario?</h5>
                <div class="campo">
                    <label>Nombre</label>
                    <input value="@Model.Veterinario.Nombre" class="campo-texto" disabled />
                </div>
                <div class="campo">
                    <label>Apellidos</label>
                    <input value="@Model.Veterinario.Apellidos" class="campo-texto" disabled />
                </div>
                <div class="campo">
                    <label>Especialidad</label>
                    <input value="@Model.Veterinario.Especialidad" class="campo-texto" disabled />
                </div>
                <div class="campo">
                    <label>Teléfono</label>
                    <input value="@Model.Veterinario.Telefono" class="campo-texto" disabled />
                </div>
                <form method="post">
                    <input type="hidden" asp-for="Veterinario.Id" />
                    <button type="submit" class="boton-guardar" style="background-color:#B71C1C;">Confirmar Eliminación</button>
                    <a asp-page="./Index" class="boton-cafe-borde d-block text-center mt-2">Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>
